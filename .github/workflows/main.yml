name: My First Workflow
on:
  push:
    branches:
      - main

jobs:
  myFirstJob:
    runs-on: ubuntu-latest
    steps:
      - name: my First Step
        run: printenv
      - name: my Second Step
        run: |
          echo "Amigoscode"
          echo "Hello" > file.txt
          ls
